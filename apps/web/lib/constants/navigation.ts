export enum SYMBOL_VIEWS {
  GRID = 'grid',
  TABLE = 'table',
}

export enum SEARCH_PARAMS {
  VIEWS = 'view',
  QUERY = 'slug',
  INTERVAL = 'interval',
  START_TIME = 'from',
  SEGMENTS = 'segments',
}

export const SEARCH_PARAM_ARRAY_SEPARATOR = ',';
