export enum SYMBOL_VIEWS {
  GRID = 'grid',
  TABLE = 'table',
}

export enum SEARCH_PARAMS_LIST_SYMBOLS {
  VIEWS = 't',
  QUERY = 'q',
}

export enum SEARCH_PARAMS_SYMBOL {
  INTERVAL = 'i',
  START_TIME = 's',
}
